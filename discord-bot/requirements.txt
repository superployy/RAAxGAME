# Clone your repo (skip if already cloned)
git clone https://github.com/superployy/RAAxGAME.git
cd RAAxGAME

# Move everything from discord-bot/ to root
mv discord-bot/* .
mv discord-bot/.env.example .   # move hidden files too
rmdir discord-bot

# Commit and push
git add -A
git commit -m "fix: move project files to repo root for Railway"
git push
